{% include 'header' %}

import Swinject

final class CoreComponentsAssembly: Assembly {
    
    func assemble(container: Container) {
        container.register(ThirdPartiesConfiguratorProtocol.self) { r in
            let thirdPartiesConfigurator = ThirdPartiesConfigurator()
            return thirdPartiesConfigurator
        }
    }
    
}
